apply plugin: "com.github.triplet.play"

play {
    serviceAccountEmail = "jaredsburrows@gmail.com"
    serviceAccountCredentials = rootProject.file(".buildscript/key.p12")
    track = "production" // or "beta" or "alpha"
}
